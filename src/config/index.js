import { isMobile as isMobileDetect } from 'react-device-detect'

export const isMobile = isMobileDetect

export const introduces = [
  'We are wandering and traveling around the world,',
  'doing some fun projects.',
  'This is the web for archiving our journey.',
]

console.log('config isMobile', isMobile)
